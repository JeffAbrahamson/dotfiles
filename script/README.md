# Scripts

This directory contains repository-maintenance helpers rather than end-user dotfiles.

## Contents

* [`lib.sh`](lib.sh) provides shared shell helpers used by other repository scripts.
* [`python-checks.sh`](python-checks.sh) runs the Python formatter, linter, and test suite in a consistent way, which is why repo instructions point here for `black`, `flake8`, and `pytest`.
