# Notion

This directory contains configuration for the Notion window manager.

## Contents

* [`notion/`](notion/README.md) holds the actual Notion config files and examples.
* [`install.sh`](install.sh) installs the configuration.
* [`test.sh`](test.sh) runs Notion-specific checks.
