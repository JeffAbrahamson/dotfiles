# Git Config

This directory contains the git configuration payload installed by the repo.

## Contents

* [`gitconfig`](gitconfig) is the main git configuration file, including aliases, defaults, and personal workflow settings.
