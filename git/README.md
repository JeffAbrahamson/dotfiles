# Git

This directory contains git configuration and the Makefile target used to install or test it.

## Contents

* [`git/`](git/README.md) holds the actual git configuration files.
* [`Makefile`](Makefile) provides the `install` and `test` targets for the git config.
