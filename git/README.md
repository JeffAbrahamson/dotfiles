# Git

This directory contains git configuration and the scripts that install or test it.

## Contents

* [`git/`](git/README.md) holds the actual git configuration files.
* [`install.sh`](install.sh) installs the git config.
* [`test.sh`](test.sh) runs git-specific checks.
