# Bin Config

This directory contains small data files used by scripts in [`../bin/`](../bin/README.md).

## Contents

* [`timezones`](timezones) is a lookup/configuration file used by timezone-aware helper scripts.
