# Bandwidth History Fixtures

This directory contains fixture files used by the bandwidth history tests.

## Contents

* The `*-ping`, `*-up`, `speedtest-*`, and `speedtest-ssid` files model raw measurement inputs for [`../README.md`](../README.md) and the older [`../../bin/bandwidth_history.py`](../../bin/bandwidth_history.py) workflow.
