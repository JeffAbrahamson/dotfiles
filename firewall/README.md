# Firewall

This directory contains firewall-related installation support.

## Contents

* [`Makefile`](Makefile) provides the `install` target that applies the firewall configuration used by this dotfiles setup.
