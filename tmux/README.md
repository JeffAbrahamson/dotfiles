# Tmux

This directory contains Tmux configuration.

## Contents

* [`tmux/`](tmux/README.md) holds the actual Tmux config files.
* [`Makefile`](Makefile) provides the `install` target for the Tmux setup.
